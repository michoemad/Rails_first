<h1> Tag Name <%= @tag.name %> </h1>
<ul>
<% @tag.articles.each do |a| %>
<li>
<%= link_to a.title, article_path(a) %>
</li>
<% end %>
</ul>

<p> <%= link_to "Delete Tag",  tag_path(@tag), method: :delete, data: {confirm: "Really delete the article?"} %> </p>